AFTER_SELECTION:
{{ template "skip_irrelevant" }}
if i.head >= len(i.str) {
	i.errc = ErrUnexpEOF
	goto ERROR
} else if i.str[i.head] == '#' {
	goto COMMENT
} else if i.str[i.head] == '}' {
	goto SEL_END
}
i.expect = ExpectSel
goto SELECTION
